DOMAIN=location.example.com
DB_PASSWORD=replace_with_random_database_password
DB_ROOT_PASSWORD=replace_with_another_random_password
# Generate with: docker run --rm -it caddy:2 caddy hash-password
# Keep single quotes so Compose does not interpolate the dollar signs.
ADMIN_HASH='$2a$14$replace_with_generated_hash'
