# 微信授权定位 + 邀请卡片：部署说明

## 本次交付

- PHP + MySQL 原项目完整源码，保留 Layui 首页、查询入口、QQ 分享字段和 QQ 分享按钮。
- 新增 share.php 邀请图片生成器：自定义标题/说明、二维码、PNG 保存和网页链接复制；不需要小程序账号。
- probe.php 使用原有页面模板，并追加显眼的定位说明和授权区域。
- 用户勾选说明 → 点击获取 → 浏览器/系统授权 → 本地预览 → 点击确认提交；每次只获取一次，不持续跟踪。
- 使用 HTTPS + navigator.geolocation，不接入微信 JS-SDK，不需要 AppID/AppSecret；不保证所有微信版本均支持标准定位，失败时有提示，可改用系统浏览器。
- 坐标按 WGS84 保存，附精度与 UTC 提交时间，不调用第三方逆地理编码，不承诺街道地址或绝对精准。
- 提交绑定服务器会话中的随机令牌，30 分钟过期；拒绝传记录 ID 任意更新；参数化查询、范围校验和同一会话令牌重复提交保护。

## 最简单的部署：Linux 服务器 + Docker Compose

前提：服务器已安装 Docker Engine 与 Compose 插件。准备独立域名，例如 location.example.com，将 A 记录指向服务器公网 IPv4；如没有正确配置 IPv6，请勿保留错误的 AAAA 记录。开放 TCP 80/443，并确保未被其他网站服务占用。

1. 上传并解压项目，进入包含 compose.yaml 的目录。
2. 复制配置并填写域名、两个不同的随机数据库密码：

```sh
cp .env.example .env
nano .env
```

3. 生成网页管理入口的密码哈希，命令会交互式询问密码：

```sh
docker run --rm -it caddy:2 caddy hash-password
```

把输出哈希填入 `.env` 的 ADMIN_HASH，保留外面的单引号，避免 `$` 被解析。记住自己输入的密码；网页管理入口用户名为 `operator`。

4. 启动并初始化原后台管理员：

```sh
docker compose up -d --build
docker compose exec web php deploy/init-admin.php
```

第二条会生成原后台 `admin` 的随机密码，仅在终端显示一次，请自行妥善保存。再次运行会重置该密码。网页入口密码与原后台 admin 密码是两套不同的密码。

5. 打开 `https://你的域名/`，先输入 operator 和第 3 步密码。首次 Caddy 会申请 HTTPS 证书，必须确保域名解析及 80/443 可达。
6. 原首页创建链接，勾选“授权定位（微信/QQ）”，选择页面模板。生成结果包含通用链接、QQ 分享入口和“生成微信邀请图片卡片”。
7. 卡片页面填入同一个密钥，点击生成，保存 PNG 图片发微信，或者复制通用网页链接发送。朋友打开定位页面不需要管理密码。
8. 在 `/probe_query.php` 输入密钥查看已确认提交的位置；后台 `/admin/login.php` 使用第 4 步生成的 admin 密码。

查看服务状态与问题：

```sh
docker compose ps
docker compose logs --tail=80 web caddy db
```

数据库保存在 db_data 命名卷。普通重启/重建保留数据；不要执行 `docker compose down -v`，它会删除数据库卷。部署前若已有数据库，请先备份，不要直接导入初始化 SQL 覆盖旧数据。

## 微信配置说明

本版生成的是小程序风格的“网页邀请图片”，不是微信原生小程序卡片。微信不会因此把普通网页链接变为小程序卡片，也不保证链接缩略图样式。图片二维码指向实际 HTTPS 网页，支持长按识别/扫码的客户端可以打开。

不需要公众号或 JS-SDK 域名配置，因为本版不调用 wx.config / wx.getLocation。若未来升级为微信 JS-SDK 方案，需要具备相应接口权限的公众号、JS 接口安全域名、服务端 ticket 缓存和当前页面 URL 签名；本包没有伪造或内置这些条件。

定位失败时检查：HTTPS 证书有效、顶层打开页面、系统定位服务开启、微信具有系统定位权限、浏览器授权未被拒绝。浏览器已记住授权时未必每次再弹系统窗口，但本页每次仍要求主动勾选、点击和确认提交。HTTP 或微信不支持时不会退回静默定位。

## 与原版的差异及保留范围

- 保留原管理界面、模板、QQ 分享字段及入口；分享链接改为 HTTPS，增加直接复制网页链接的入口。
- 为满足显式授权要求，本版不再在打开页面时自动创建访客记录或调用 IP 地址定位；仅在确认提交位置后写入基础信息及坐标。旧 IP 定位/摄像头/邮件选项仍保留在原界面，但本次新的提交链路不执行这些旧采集和邮件功能，勿将其视为已集成验证的功能。
- 原 gps_where.php 的服务端请求脚本流程已替换，旧 s/ip_id 写入协议不再支持。
- 原源码多处存在无条件退出错误，已修复；原“授权检查”只有无返回结果的 HTTP 请求，不构成授权校验，改为空兼容钩子。停用原 HTTP 自更新，防止覆盖修改版。
- 删除分发包中原附带的历史数据库转储和嵌套压缩包；原上传 ZIP 未改动。数据库配置改从环境变量读取。
- Caddy 将创建、查询、管理、API 与卡片生成入口置于统一密码保护之后；访客定位页、定位提交接口及必要静态资源公开。不要绕过 Caddy 直接把 PHP 端口开放公网。
- 原后台密码改为哈希验证。迁移旧数据库后使用 init-admin.php 重置 admin；其他旧明文密码账号需另行迁移。
- 原项目属于旧版代码，本次并非整个后台的全面安全重写；推荐按随包 Caddy 配置部署，仅让可信管理员使用管理入口。
- 若使用宝塔或自行安装 PHP，请使用 PHP 8.3+、mysqli、MySQL/MariaDB，网站根目录指向项目根目录，配置与 Caddy 相同的访问保护和 HTTPS。新库导入 deploy/init.sql，再按配置提供 DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME 环境变量。不要使用旧网页安装向导覆盖环境配置。

## 验证与验收

已完成：全部 PHP 文件语法检查；新增 JavaScript 语法检查；真实桌面浏览器卡片生成、PNG 导出、输入变更清除旧预览、HTTP 地址拒绝、390px 手机宽度无横向溢出；模拟浏览器定位的未授权、预览未提交、确认提交、拒绝授权、超时、取消后迟到回调、撤回勾选测试。

未完成：本机 Docker 未运行，未实测容器启动、真实 MySQL 写入、服务器证书签发、微信/QQ 手机定位和实际转发效果。浏览器定位测试使用模拟坐标与模拟提交成功响应，不是微信真机或数据库端到端证明。

上线验收：自己先在微信打开测试链接 → 未点击时不得弹定位/写位置 → 拒绝时显示失败 → 同意后只在本页预览 → 确认提交后管理查询出现一条记录 → 连点/重试不重复 → 另一设备不能用同一写入令牌覆盖记录。

邀请卡片示例图片中的 location.example.com 为演示域名，不是已部署地址。

参考：
- https://developer.mozilla.org/en-US/docs/Web/API/Geolocation/getCurrentPosition
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Permissions-Policy/geolocation
- 二维码本地依赖 qrcode-generator 2.0.4（MIT）：https://github.com/kazuhikoarase/qrcode-generator ，许可随 assets/qrcode-LICENSE.txt 分发。
